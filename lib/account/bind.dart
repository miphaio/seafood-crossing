import 'package:flutter/material.dart';

class BindAccount extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('bind'),
      ),
      body: Center(
        child: Text('Example'),
      ),
    );
  }
}
