import 'package:flutter/material.dart';

class CollectableIndex extends StatelessWidget {
  static const String route = '/collecatble';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
